name: Update Dependencies
on: push
  #  schedule:
  #    - cron: "0 0 * * 0"
jobs:
  update_deps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Limine Binary
        uses: actions/github-script@v7
        with:
          script: |
            var latestTags = await github.rest.repos.listTags({ owner: "limine-bootloader", repo: "limine", per_page: 2 })
            console.log(latestTags)
