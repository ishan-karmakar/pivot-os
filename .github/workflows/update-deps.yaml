name: Update Dependencies
on: workflow_dispatch
  #  schedule:
  #    - cron: "0 0 * * 0"
jobs:
  update_deps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/github-script@v7
        with:
          script: |
            console.log(octokit.rest.repos.listReleases({ owner: "limine-bootloader", repo: "limine" })
