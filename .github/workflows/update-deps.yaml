name: Update Dependencies
on: push
  #  schedule:
  #    - cron: "0 0 * * 0"
jobs:
  update_deps:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Limine Binary
        uses: actions/github-script@v7
        with:
          script: |
            var latestRelease = await github.rest.repos.listReleases({ owner: "limine-bootloader", repo: "limine", per_page: 1 })
            console.log(latestRelease)
