# DiskMaker config - CMake will fill in most of these at configure time

# Target files
BIOS1=bios/bios1.bin
BIOS2=bios/bios2.bin
EFI=$<TARGET_FILE:uefi>
ELF=$<TARGET_FILE:kernel>
OUT=os.img

# Executable paths
FAT_MKFS=${FAT_MKFS}
MMD=${MMD}
MCOPY=${MCOPY}
SGDISK=${SGDISK}

# FAT paths
EFI_PATH=/EFI/BOOT
ELF_PATH=/
NUM_SUBDIRECTORIES=1